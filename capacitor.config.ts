import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'angular_firebase_ionic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
